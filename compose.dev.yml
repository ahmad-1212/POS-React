services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    env_file:
      - .env
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - 5000:5000
    volumes:
      - .:/client
      - /client/node_modules
